# Dev: API via Vite proxy (same origin). Use https:// in browser (required for mobile camera).
VITE_API_PORT=3000

# Production only:
# VITE_API_BASE_URL=https://api.yourdomain.com

VITE_RAZORPAY_KEY_ID=
