import DigitalEntryPass from "./DigitalEntryPass";

/** @deprecated Use DigitalEntryPass — kept for backward-compatible imports */
export default function UserProfile() {
  return <DigitalEntryPass />;
}
